import { OperationRouteDiagramFormatStationNamePipe } from './operation-route-diagram-format-station-name.pipe';

describe('OperationRouteDiagramFormatStationNamePipe', () => {
  it('create an instance', () => {
    const pipe = new OperationRouteDiagramFormatStationNamePipe();
    expect(pipe).toBeTruthy();
  });
});
