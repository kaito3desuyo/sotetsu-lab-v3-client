import { TimetableStationFindLastStopStationPipe } from './timetable-station-find-last-stop-station.pipe';

describe('TimetableStationFindLastStopStationPipe', () => {
  it('create an instance', () => {
    const pipe = new TimetableStationFindLastStopStationPipe();
    expect(pipe).toBeTruthy();
  });
});
