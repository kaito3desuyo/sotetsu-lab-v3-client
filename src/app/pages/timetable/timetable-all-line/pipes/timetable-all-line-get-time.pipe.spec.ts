import { TimetableAllLineGetTimePipe } from './timetable-all-line-get-time.pipe';

describe('TimetableAllLineGetTimePipe', () => {
  it('create an instance', () => {
    const pipe = new TimetableAllLineGetTimePipe();
    expect(pipe).toBeTruthy();
  });
});
