import { TimetableAllLineGetBorderSettingPipe } from './timetable-all-line-get-border-setting.pipe';

describe('TimetableAllLineGetBorderSettingPipe', () => {
  it('create an instance', () => {
    const pipe = new TimetableAllLineGetBorderSettingPipe();
    expect(pipe).toBeTruthy();
  });
});
